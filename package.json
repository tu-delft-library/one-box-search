{
  "name": "one-box-search",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "json-2-csv": "^5.5.9"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun run build.ts",
    "watch": "bun build ./src/index.ts --outdir ./dist --entry-naming search.js --watch"
  }
}
